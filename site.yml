---
- name: install common utilities
  hosts: all
  become: yes

  tasks:
    - name: install common
      community.general.pkgng:
        name: "{{ item }}"
        state: latest
      with_items:
        - tmux
        - htop

- name: configure ipfw
  hosts: all
  become: yes
  roles:
    - ipfw

- name: install fail2ban
  hosts: all
  become: yes
  roles:
    - fail2ban

- name: weekly reboot
  hosts: all
  become: yes
  roles:
    - reboot
